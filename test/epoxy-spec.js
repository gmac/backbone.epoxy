// Epoxy.Model
// -----------
describe("Backbone.Epoxy.Model", function() {
	
	// Setup
	beforeEach(function() {
		
	});
	
	// Teardown
	afterEach(function() {
		// do nothing.
	});
	
	it("should define a list of computed properties.", function() {
		expect( null ).toBe( 1 );
	});
	
	it("should define computed properties individually.", function() {
		expect( null ).toBe( 1 );
	});
	
	it("should get a compiled value for a computed property.", function() {
		expect( null ).toBe( 1 );
	});
	
	it("should get a compiled value for a computed property.", function() {
		expect( null ).toBe( 1 );
	});
});



// Epoxy.View
// ----------
describe("Backbone.Epoxy.View", function() {
	
	// Setup
	beforeEach(function() {
		
	});
	
	// Teardown
	afterEach(function() {
		// do nothing.
	});
	
	it("addNode: should add a new node with specified X and Y coordinates, and return its id.", function() {
		expect( null ).toBe( 1 );
	});
	
});